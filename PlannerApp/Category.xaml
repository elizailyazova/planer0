<Window x:Class="PlannerApp.Category"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PlannerApp.ViewModels"
        Title="Category" 
        WindowStartupLocation="CenterScreen" WindowState="Maximized"
        Height="1024" Width="1440" Background="#F5F8FF">

    <Window.DataContext>
        <local:CategoryViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <local:TakeFirstTwoConverter x:Key="TakeFirstTwoConverter"/>
        <DropShadowEffect x:Key="CardShadowEffect"
                      ShadowDepth="5"
                      Opacity="0.3"
                      BlurRadius="10"
                      Color="#000000" />
    </Window.Resources>

    <Grid>
        <!-- Верхнее меню -->
        <DockPanel Height="100" Background="#F8F8F8" VerticalAlignment="Top" Margin="0,0,0,10">
            <DockPanel.Effect>
                <DropShadowEffect Color="Gray" BlurRadius="10" ShadowDepth="3" Opacity="0.3"/>
            </DockPanel.Effect>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="90,30,0,0">
                <TextBlock Text="Task" Foreground="#41AB49" FontSize="32" FontWeight="SemiBold"/>
                <TextBlock Text="Track" Foreground="Black" FontSize="32" FontWeight="SemiBold" Margin="5,0,0,0"/>
            </StackPanel>
            <Grid Width="695" Height="40" Margin="166,0" HorizontalAlignment="Center">
                <TextBox x:Name="SearchTextBox" 
                         Width="650" 
                         Height="40"
                         Background="#F5F8FF"
                         Margin="0,0,45,0"
                         Padding="10"
                         VerticalContentAlignment="Center"
                         Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8"
                                                BorderThickness="1"
                                                BorderBrush="#A1A3AB"
                                                Focusable="True">
                                            <ScrollViewer x:Name="PART_ContentHost"
                                                        VerticalAlignment="Center"
                                                        Padding="{TemplateBinding Padding}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                    <TextBox.Effect>
                        <DropShadowEffect Color="Gray" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                    </TextBox.Effect>
                </TextBox>

                <!-- Кнопка поиска -->
                <Button x:Name="SearchButton" 
                        Width="40" 
                        Height="40" 
                        Background="#41AB49" 
                        Foreground="White" 
                        BorderThickness="0"
                        HorizontalAlignment="Right"
                        Click="SearchButton_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                    <Image Source="/images/search.png" Width="15" Height="15"/>
                </Button>
            </Grid>            
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,90,0">
                <Button Width="40" Height="40" Background="#41AB49" Foreground="White" BorderThickness="0"
                        HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                    <Image Source="images/notification.png" Width="15" Height="15"/>
                </Button>

                <Button Click="ShowCalendar" Width="40" Height="40" Background="#41AB49" Foreground="White" BorderThickness="0" Margin="5,0"
                        HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                    <Image Source="images/calendar.png" Width="15" Height="15"/>
                </Button>
  
        <StackPanel Orientation="Vertical" HorizontalAlignment="Right" Margin="10,30">
            <TextBlock Text="{Binding CurrentDay}" Foreground="Black" FontSize="15" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding CurrentDate}" Foreground="#3ABEFF" FontSize="14" HorizontalAlignment="Center"/>
        </StackPanel>
    </StackPanel>
</DockPanel>

        <Grid Margin="0,80,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="365"/>  
                <ColumnDefinition Width="1050"/>   
            </Grid.ColumnDefinitions>

            <!-- Боковое меню -->

            <Grid Height="640"  VerticalAlignment="Bottom">
                <Border Background="#408057" CornerRadius="8"
                        BorderBrush="#A1A3AB" BorderThickness="1">
                    
                    <StackPanel Margin="20">
                        <StackPanel VerticalAlignment="Bottom" Margin="0,40,0,0">
                            <TextBlock Name="UsernameText" Text="Asel" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" />
                            <TextBlock Name="EmailText" Text="asel@gmail.com" Foreground="White" FontSize="12" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <Button Background="Transparent" Foreground="White" FontSize="16" Height="59" BorderThickness="0" HorizontalAlignment="Left" Margin="50,30,0,0">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="/Images/dashboard.png" Width="15" Height="15" Margin="0,0,10,0"/>
                                <TextBlock Text="Dashboard" VerticalAlignment="Center" Width="118"/>
                            </StackPanel>
                        </Button>

                        <Button Background="Transparent" Foreground="White" FontSize="16" Height="59" BorderThickness="0" HorizontalAlignment="Left" Margin="50,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="images/statitics.png" Width="15" Height="15" Margin="0,0,10,0"/>
                                <TextBlock Text="Statistics" VerticalAlignment="Center" Width="118"/>
                            </StackPanel>
                        </Button>

                        <Button Click="OpenAddTask" Background="Transparent" Foreground="White" FontSize="16" Height="59" BorderThickness="0" HorizontalAlignment="Left" Margin="50,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="images/my task.png" Width="15" Height="15" Margin="0,0,10,0"/>
                                <TextBlock Text="My Task" VerticalAlignment="Center" Width="116"/>
                            </StackPanel>
                        </Button>

                        <Button Background="Transparent" Foreground="White" FontSize="16" Height="59" BorderThickness="0" HorizontalAlignment="Left" Margin="50,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="images/mycategories.png" Width="15" Height="15" Margin="0,0,10,0"/>
                                <TextBlock Text="Task Categories" VerticalAlignment="Center" Width="113"/>
                            </StackPanel>
                        </Button>

                        <Button  Background="Transparent" Foreground="White" FontSize="16" Height="59" BorderThickness="0" HorizontalAlignment="Left" Margin="50,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="images/settings.png" Width="15" Height="15" Margin="0,0,10,0"/>
                                <TextBlock Text="My Profile" VerticalAlignment="Center" Width="116"/>
                            </StackPanel>
                        </Button>

                        <Button Click="Logout" Background="Transparent" Foreground="White" FontSize="16" Height="66" BorderThickness="0"  VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="50,60,0,0">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="images/logout.png" Width="15" Height="15" Margin="0,0,10,0"/>
                                <TextBlock Text="Logout"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>

            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="10,0,0,0">
                <StackPanel Margin="30,30,0,20">
                    <TextBlock Text="Task Categories" FontSize="26" FontWeight="SemiBold" Margin="5,10,0,20" Foreground="#333"/>

                    <ItemsControl ItemsSource="{Binding TaskGroups}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F4F4F8" CornerRadius="15" Padding="15" Margin="10" Width="400" 
                            BorderBrush="#E0E0E0" BorderThickness="1" Effect="{StaticResource CardShadowEffect}">
                                    <StackPanel>
                                        <Button Style="{StaticResource NoBackgroundButtonStyle}"
                                    Command="{Binding OpenCategoryCommand}"
                                    CommandParameter="{Binding}">
                                            <Button Content="{Binding Category}" Background="#F4F4F8" Click="CategoryClick" FontWeight="SemiBold" 
                                                    FontSize="20" Margin="0,0,0,10"  BorderThickness="0"/>
                                        </Button>

                                        <ScrollViewer Height="260" VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding Tasks}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Background="#FFFFFF" CornerRadius="10" BorderBrush="#DADADA" BorderThickness="1" Padding="10" Margin="5">
                                                            <StackPanel>
                                                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,5"/>
                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*" />
                                                                        <ColumnDefinition Width="Auto" />
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBlock Text="{Binding Date, StringFormat='{}{0:dd MMM yyyy}'}" Foreground="#41AB49"
                                                                               FontWeight="Normal" Grid.Column="0"/>
                                                                    <TextBlock Text="{Binding Time, StringFormat='{}{0:hh\\:mm}'}" Foreground="#41AB49" Margin="10,0,0,0" 
                                                                               Grid.Column="1" FontWeight="Normal"/>
                                                                </Grid>
                                                            </StackPanel>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>

                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>

        </Grid>
    </Grid>
</Window>
